import{testIntegration}from'@/lib/integrations';export const runtime='nodejs';
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){if((request.headers.get('x-ddf-role')||(process.env.NODE_ENV==='production'?'':'ADMIN'))!=='ADMIN')return Response.json({error:'Permissão insuficiente.'},{status:403});try{const{id}=await params;const result=await testIntegration(id);return result?Response.json(result,{status:result.success?200:422}):Response.json({error:'Integração não encontrada.'},{status:404})}catch(error){return Response.json({error:error instanceof Error?error.message:'Teste indisponível.'},{status:503})}}
