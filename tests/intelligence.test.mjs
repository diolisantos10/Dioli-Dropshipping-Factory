import test from 'node:test';import assert from 'node:assert/strict';import {buildIntelligence} from '../src/lib/intelligence.ts';
const intake={version:1,candidates:[{id:'c1',name:'A',url:'https://a.test/',notes:'',status:'APROVADO',createdAt:'x',source:'TREND',category:'Moda'}],events:[]};
const products={version:1,products:[{id:'p1',candidateId:'c1',status:'PRONTO',version:1,universalTitle:'Produto A',shortDescription:'',longDescription:'',category:'Óculos',bullets:[],benefits:[],tags:[],createdAt:'x',updatedAt:'x'}],events:[]};
const pricing={version:1,calculations:[{id:'pc',productId:'p1',offerId:'of1',approval:'APROVADO'}]};
test('intelligence reconcilia dimensões e feedback sem comando',()=>{const orders={version:1,processedExternalIds:[],orders:[{id:'o1',externalOrderId:'1',productId:'p1',status:'ENVIADO',salePrice:100,costSnapshot:70,currency:'BRL',tracking:'t',createdAt:'2026-09-20T00:00:00Z',updatedAt:'x'}]};const r=buildIntelligence(intake,products,pricing,orders,new Date('2026-09-22T00:00:00Z'));assert.deepEqual([r.totals.revenue,r.totals.cost,r.totals.contribution,r.totals.marginPercent],[100,70,30,30]);assert.equal(r.byOrigin[0].label,'TREND');assert.equal(r.byOffer[0].label,'of1');assert.equal(r.trend.label,'SEM_JANELA');assert.ok(r.feedback.every(x=>!x.toLowerCase().includes('executar')))});
test('ausência é sem dados, não zero analítico',()=>{const r=buildIntelligence(intake,products,pricing,{version:1,processedExternalIds:[],orders:[]},new Date('2026-09-22T00:00:00Z'));assert.equal(r.totals.marginPercent,null);assert.equal(r.trend.label,'SEM_DADOS')});
